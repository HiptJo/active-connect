import { FileProvider } from "./file-provider";

/**
 * Represents an image provider for serving images.
 * Inherits from the FileProvider class.
 */
export class ImageProvider extends FileProvider {}
